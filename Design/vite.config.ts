import { defineConfig } from 'vite'
import path from 'path'
import tailwindcss from '@tailwindcss/vite'
import react from '@vitejs/plugin-react'
import { resolveAppVersion } from '../scripts/app-version'

const appVersion = resolveAppVersion(path.resolve(__dirname, '..'))

export default defineConfig({
  define: {
    __APP_VERSION__: JSON.stringify(appVersion.name),
    __APP_VERSION_CODE__: JSON.stringify(appVersion.code),
  },
  plugins: [
    // The React and Tailwind plugins are both required for Make, even if
    // Tailwind is not being actively used – do not remove them
    react(),
    tailwindcss(),
  ],
  resolve: {
    alias: {
      // Alias @ to the src directory
      '@': path.resolve(__dirname, './src'),
    },
  },

  // File types to support raw imports. Never add .css, .tsx, or .ts files to this.
  assetsInclude: ['**/*.svg', '**/*.csv'],
})
