Code layer isn’t working
Open code editor to review issues
Design System for TideTunes
File name
Beta
Version 1
Reasoning
Worked with 11 files
Reasoning

Let me check that the main CSS entry point imports fonts.css:

Reasoning

Good — fonts.css is already imported. Let me also check if there's anything in globals.css that might conflict:

Reasoning

Let me clean up the unused useRef import:

The implementation is complete. Here's what's been built:

TideTunes Design System

A comprehensive, production-quality design system and music app prototype with the following architecture:

Design Tokens (theme.css)
Brand palette: TidePink #FF5B8A · TidePurple #7A6CFF · Orange · Green · Blue · Yellow
8 gradient pairs using brand + support colors
Radius scale: 12 / 20 / 28 / 36px
Full dark/light mode via CSS custom properties
Plus Jakarta Sans (variable weight display) + JetBrains Mono (badges/data)
Component Library
Category	Components
Primitives	TideButton (5 variants), TideSwitch, TideSlider, TideTabs (3 variants), QualityBadge
Music Cards	AlbumCard, ArtistCard, PlaylistCard, MusicCard, SourceCard
Layout	SectionHeader, SettingsSection, SettingsItem, ListItem, EmptyState, Toast, SkeletonBlock
Player	MiniPlayer (frosted glass, spring animation), FullPlayer (blur bg, lyrics/queue/EQ tabs)
App Pages
Home — rotating hero banner, horizontal scroll sections (albums, artists, playlists, recent)
Search — live filter, category grid, trending songs
Library — tabbed: Songs, Albums, Artists, Playlists, Folders, Sources
Settings — all settings groups with live controls (switches, sliders)
Design System Docs
Foundation — color swatches, gradient pairs, type scale, spacing blocks, radius demo, elevation levels
Components — every component showcased with all variants in a clean grid
Patterns — Mini Player, Nav Bar, Nav Rail, Top App Bar, device layout cards
Layout Behavior
Desktop (lg+): 240px sidebar with App + DS navigation sections
Mobile: Bottom navigation bar with active indicator animation
Dark mode toggle in sidebar / top bar
Design System for TideTunes
Version 1
Sign up to use Figma Make
Sign up




Ask for changes
Edit
Build
Default
/
Mobile preview
Share
This website uses cookies, pixel tags, and local storage for performance, personalization, and marketing purposes. We use our own cookies and some from third parties. Only essential cookies are turned on by default. Cookies settings
Do not allow cookies
Allow all cookies